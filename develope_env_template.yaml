# change the content fit your environment and rename this file to develope_env.yaml.
# sudo apt-get install openssh-server
# change virtualbox network to host-only.
# E:\Program Files\Oracle\VirtualBox>
# .\VBoxManage.exe modifyvm "ubuntubionic" --natdnshostresolver1 on
# sudo apt install pkg-config libssl-dev
# https://mirrors.sjtug.sjtu.edu.cn/git/crates.io-index/
# https://docs.microsoft.com/en-us/windows/wsl/user-support
# https://docs.microsoft.com/en-us/windows/wsl/wsl2-install

# New-NetFirewallRule -DisplayName 'wsl sshd 2222' -Profile @('Domain', 'Private') -Direction Inbound -Action Allow -Protocol TCP -LocalPort @('2222')

# sudo /usr/bin/ssh-keygen -A // Could not load host key: /etc/ssh/ssh_host_rsa_key

# Port 2222
# PermitRootLogin yes
# PasswordAuthentication yes

# sudo apt install build-essential, this will install gcc.

# sudo fuser -vki /var/lib/dpkg/lock
# sudo apt purge snapd
# sudo dpkg --configure -a
# sudo apt update
# sudo apt upgrade
# sudo apt dist-upgrade
# sudo apt install snapd
# libssl-dev
# sudo apt install pkg-config



ssh_client:
    id_rsa: C:\Users\Administrator\.ssh\id_rsa
    id_rsa_pub: C:\Users\Administrator\.ssh\id_rsa.pub
servers:
  ubuntu18:
    host: 192.168.56.101:22
    username: osboxes
    test_dirs:
      aatxt: /home/osboxes/ws/ssh-client-demo/fixtures/aa.txt
      linux_remote_item_dir: /home/osboxes/ws/ssh-client-demo/fixtures/linux_remote_item_dir.json
    test_files:
      big_binary_file: /home/osboxes/Downloads/CentOS-7-x86_64-DVD-1810.iso
      midum_binary_file: /home/osboxes/Downloads/CentOS-7-x86_64-Minimal-1810.iso
